# uberminimal

```bash
#first install everything
npm install

#then start webpack watching
npm run dev

#once you have a server bundle, start the server in another tab, this will refresh whenever the server bundle is remade
npm run server
```
